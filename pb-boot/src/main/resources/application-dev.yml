server:
  port: 32223

spring:
  datasource:
    url: jdbc:mysql://47.108.157.243:3306/probsolve-n?useUnicode=true&characterEncoding=utf-8&allowMultiQueries=true&allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=Asia/Shanghai
    username: probsolve-n
    password: zdYmEwfPRKiGZwCz
    driver-class-name: com.mysql.cj.jdbc.Driver

#langchain4j:
#  open-ai:
#    chat-model:
#      base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
#  community:
#    dashscope:
#      chat-model:
#        api-key: sk-5bf3bddeabda4fd8b919299b352300b1
#        model-name: qwen3-max

# 应用配置
app:
  # Token 配置
  token:
    # Token 密钥
    secret: probsolve-token-secret-key
    # 过期配置
    expire:
      # 过期时间
      value: 86400
      # 持久化过期时间
      persistent: 604800
  # 上传配置
  upload:
    # 上传路径
    path: ./upload/path
  # 密钥配置
  secret:
    # 公钥
    publicKey: ${APP_ENCRYPT_RSA_PUBLIC_KEY}
    # 私钥
    privateKey: ${APP_ENCRYPT_RSA_PRIVATE_KEY}